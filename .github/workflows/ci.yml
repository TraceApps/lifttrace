name: Checks

# The repo's other workflow (docker.yml) builds and publishes images; keeping
# checks separate means a red check never blocks an image push and a slow
# multi-arch build never delays PR feedback.
on:
  pull_request:
  push:
    branches: [ main, dev ]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Node 20 to match node:20-alpine in the Dockerfile.
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install (root)
        run: npm ci

      # server/ has its own package.json (better-sqlite3 etc.). The dedupe
      # migration tests use better-sqlite3 via createRequire so they exercise
      # the same driver production runs against.
      - name: Install (server)
        run: npm ci
        working-directory: server

      - name: i18n check
        run: npm run i18n:check

      - name: Tests
        run: npm test
